name: festive_crops_app
description: Smart Crop Recommendation with Festival Demand
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4

  geolocator: ^14.0.2
  geocoding: ^4.0.0
  image_picker: ^1.1.2
  dio: ^5.7.0
  intl: ^0.20.2
  flutter_spinkit: ^5.2.1

  # THESE 2 LINES ARE NOW ADDED — FIXES ALL RED ERRORS
  http: ^1.2.2
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/